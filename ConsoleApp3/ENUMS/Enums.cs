using System;
using System.Collections.Generic;
using System.Diagnostics;
using System.Text;

internal enum Subscription { Free, Premium }
internal enum Role { User, Admin }
internal enum RequestType { Pending, Approved, Rejected }

